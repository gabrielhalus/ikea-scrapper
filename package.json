{
  "name": "ikea-scrapper",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "check": "bun run src/index.ts --once"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
